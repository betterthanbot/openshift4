---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ctfd
  namespace: ctfd
spec:
  to:
    kind: Service
    name: ctfd-web
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
