{{- if and .Values.logging .Values.logging.enabled .Values.logging.clusterLogForwarder .Values.logging.clusterLogForwarder.enabled }}
{{- $clf := .Values.logging.clusterLogForwarder }}
---
# ServiceAccount used by the vector log collector pods.
# ClusterLogForwarder will not become Ready without this SA existing first.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $clf.serviceAccountName }}
  namespace: {{ $clf.namespace }}
  labels:
    {{- include "layer2-operands.labels" . | nindent 4 }}
---
# Grants the collector SA permission to collect application logs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: logging-collector-application-logs
  labels:
    {{- include "layer2-operands.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ $clf.serviceAccountName }}
    namespace: {{ $clf.namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: collect-application-logs
---
# Grants the collector SA permission to collect infrastructure logs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: logging-collector-infrastructure-logs
  labels:
    {{- include "layer2-operands.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ $clf.serviceAccountName }}
    namespace: {{ $clf.namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: collect-infrastructure-logs
---
# Grants the collector SA permission to collect audit logs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: logging-collector-audit-logs
  labels:
    {{- include "layer2-operands.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ $clf.serviceAccountName }}
    namespace: {{ $clf.namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: collect-audit-logs
{{- end }}
