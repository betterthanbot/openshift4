---
# Source: litemaas/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: litemaas
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
---
# Source: litemaas/templates/backend-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: litemaas-backend
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: api
type: Opaque
stringData:
  database-url: "postgresql://litemaas_admin:tBwJDvtAMbTzzg9T7Ki935RzhDHE9ZRHiv/SmhvWdLI=@litemaas-postgresql:5432/litemaas_db?sslmode=disable"
  cors-origin: "*"
  jwt-secret: "tBwJDvtAMbTzzg9T7Ki935RzhDHE9ZRHiv/SmhvWdLI="
  oauth-client-id: "system:serviceaccount:litemaas:litemaas"
  oauth-client-secret: ""
  oauth-issuer: ""
  oauth-callback-url: ""
  admin-api-keys: "tBwJDvtAMbTzzg9T7Ki935RzhDHE9ZRHiv/SmhvWdLI="
  litellm-api-key: "sk-masterKey"
  litellm-master-key: "sk-masterKey"
  litellm-database-url: "postgresql://litemaas_admin:tBwJDvtAMbTzzg9T7Ki935RzhDHE9ZRHiv/SmhvWdLI=@litemaas-postgresql:5432/litellm_db"
---
# Source: litemaas/templates/litellm-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: litemaas-litellm
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: ai-proxy
type: Opaque
stringData:
  database-url: "postgresql://litemaas_admin:tBwJDvtAMbTzzg9T7Ki935RzhDHE9ZRHiv/SmhvWdLI=@litemaas-postgresql:5432/litellm_db"
  master-key: "sk-masterKey"
  ui-username: "admin"
  ui-password: "admin"
---
# Source: litemaas/templates/oauth-token-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: litemaas-oauth-token
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: oauth
  annotations:
    kubernetes.io/service-account.name: litemaas
type: kubernetes.io/service-account-token
---
# Source: litemaas/templates/postgres-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: litemaas-postgresql
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: database
type: Opaque
stringData:
  username: "litemaas_admin"
  password: "tBwJDvtAMbTzzg9T7Ki935RzhDHE9ZRHiv/SmhvWdLI="
---
# Source: litemaas/templates/backend-configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: litemaas-backend-config
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: api
data:
  HOST: "0.0.0.0"
  PORT: "8080"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  JWT_EXPIRES_IN: "24h"
  RATE_LIMIT_MAX: "1000"
  RATE_LIMIT_TIME_WINDOW: "5m"
  DEFAULT_USER_MAX_BUDGET: "100"
  DEFAULT_USER_TPM_LIMIT: "100000"
  DEFAULT_USER_RPM_LIMIT: "120"
  LITELLM_API_URL: "http://litemaas-litellm:4000"
  BACKUP_STORAGE_PATH: "/data/backups"
---
# Source: litemaas/templates/postgres-init-configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: litemaas-postgresql-init
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: database
data:
  init-db.sh: |
    #!/bin/bash
    set -e

    # This script runs as the postgres user during container initialization
    # It creates additional databases beyond the default POSTGRES_DB

    echo "Creating additional databases..."

    # Create litemaas_db if it doesn't exist
    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "postgres" <<-EOSQL
        SELECT 'CREATE DATABASE litemaas_db'
        WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'litemaas_db')\gexec
    EOSQL

    # Create litellm_db if it doesn't exist
    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "postgres" <<-EOSQL
        SELECT 'CREATE DATABASE litellm_db'
        WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'litellm_db')\gexec
    EOSQL

    echo "Granting privileges on databases..."

    # Grant all privileges to the postgres user on both databases
    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "postgres" <<-EOSQL
        GRANT ALL PRIVILEGES ON DATABASE litemaas_db TO $POSTGRES_USER;
        GRANT ALL PRIVILEGES ON DATABASE litellm_db TO $POSTGRES_USER;
    EOSQL

    echo "Database initialization completed successfully!"
---
# Source: litemaas/templates/backend-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: litemaas-backend-backup
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: api
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
# Source: litemaas/templates/backend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: litemaas-backend
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
---
# Source: litemaas/templates/frontend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: litemaas
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: web
spec:
  selector:
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
---
# Source: litemaas/templates/litellm-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: litemaas-litellm
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: ai-proxy
spec:
  selector:
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: ai-proxy
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
---
# Source: litemaas/templates/postgres-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: litemaas-postgresql
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: database
spec:
  selector:
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: database
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
---
# Source: litemaas/templates/backend-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: litemaas-backend
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: litemaas
      app.kubernetes.io/instance: litemaas
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        helm.sh/chart: litemaas-0.1.0
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: litemaas
        app.kubernetes.io/version: "0.3.1"
        app.kubernetes.io/name: litemaas
        app.kubernetes.io/instance: litemaas
        app.kubernetes.io/component: api
    spec:
      serviceAccountName: litemaas
      initContainers:
        - name: wait-for-database
          image: postgres:16-alpine
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          command:
            - sh
            - -c
            - |
              until psql "$DATABASE_URL" -c '\q' 2>/dev/null; do
                echo "Waiting for database..."
                sleep 2
              done
              echo "Database is ready!"
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: database-url
          resources:
            requests:
              memory: "64Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "100m"
      containers:
        - name: backend
          image: "quay.io/rh-aiservices-bu/litemaas-backend:0.3.1"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: litemaas-backend-config
          env:
            # Database Configuration
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: database-url
            # CORS Configuration
            - name: CORS_ORIGIN
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: cors-origin
            # JWT Configuration
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: jwt-secret
            # OAuth Configuration
            - name: OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: oauth-client-id
            - name: OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: litemaas-oauth-token
                  key: token
            - name: OAUTH_ISSUER
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: oauth-issuer
            - name: OAUTH_CALLBACK_URL
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: oauth-callback-url
            # Admin Configuration
            - name: ADMIN_API_KEYS
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: admin-api-keys
            # LiteLLM Integration
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: litellm-api-key
            - name: LITELLM_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: litellm-master-key
            # Backup & Restore — LiteLLM database direct connection
            - name: LITELLM_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: litemaas-backend
                  key: litellm-database-url
          volumeMounts:
            - name: backup-storage
              mountPath: /data/backups
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: backup-storage
          persistentVolumeClaim:
            claimName: litemaas-backend-backup
---
# Source: litemaas/templates/frontend-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: litemaas
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: litemaas
      app.kubernetes.io/instance: litemaas
      app.kubernetes.io/component: web
  template:
    metadata:
      labels:
        helm.sh/chart: litemaas-0.1.0
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: litemaas
        app.kubernetes.io/version: "0.3.1"
        app.kubernetes.io/name: litemaas
        app.kubernetes.io/instance: litemaas
        app.kubernetes.io/component: web
    spec:
      serviceAccountName: litemaas
      initContainers:
        - name: wait-for-backend
          image: image-registry.openshift-image-registry.svc:5000/openshift/tools:latest
          command: ['sh', '-c']
          args:
            - |
              echo "Waiting for backend to be ready..."
              until nc -z litemaas-backend 8080; do
                echo "Backend is not ready yet. Waiting..."
                sleep 5
              done
              echo "Backend is ready! Starting frontend..."
          resources:
            requests:
              memory: "64Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "100m"
      containers:
        - name: frontend
          image: "quay.io/rh-aiservices-bu/litemaas-frontend:0.3.1"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: BACKEND_URL
              value: "http://litemaas-backend:8080"
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
# Source: litemaas/templates/litellm-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: litemaas-litellm
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: ai-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: litemaas
      app.kubernetes.io/instance: litemaas
      app.kubernetes.io/component: ai-proxy
  template:
    metadata:
      labels:
        helm.sh/chart: litemaas-0.1.0
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: litemaas
        app.kubernetes.io/version: "0.3.1"
        app.kubernetes.io/name: litemaas
        app.kubernetes.io/instance: litemaas
        app.kubernetes.io/component: ai-proxy
    spec:
      serviceAccountName: litemaas
      initContainers:
        - name: wait-for-database
          image: postgres:16-alpine
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          command:
            - sh
            - -c
            - |
              until psql "$DATABASE_URL" -c '\q' 2>/dev/null; do
                echo "Waiting for database..."
                sleep 2
              done
              echo "Database is ready!"
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: litemaas-litellm
                  key: database-url
          resources:
            requests:
              memory: "64Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "100m"
      containers:
        - name: litellm
          image: "ghcr.io/berriai/litellm-non_root:main-v1.81.0-stable"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4000
              name: http
          env:
            # Database Configuration
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: litemaas-litellm
                  key: database-url
            # LiteLLM Configuration
            - name: LITELLM_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: litemaas-litellm
                  key: master-key
            - name: UI_USERNAME
              valueFrom:
                secretKeyRef:
                  name: litemaas-litellm
                  key: ui-username
            - name: UI_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: litemaas-litellm
                  key: ui-password
            # Application Configuration
            - name: LITELLM_MODE
              value: "PRODUCTION"
            - name: STORE_MODEL_IN_DB
              value: "True"
          resources:
            limits:
              cpu: 1000m
              memory: 1Gi
            requests:
              cpu: 200m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 4000
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 4000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
---
# Source: litemaas/templates/postgres-statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: litemaas-postgresql
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: database
spec:
  serviceName: litemaas-postgresql
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: litemaas
      app.kubernetes.io/instance: litemaas
      app.kubernetes.io/component: database
  template:
    metadata:
      labels:
        helm.sh/chart: litemaas-0.1.0
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: litemaas
        app.kubernetes.io/version: "0.3.1"
        app.kubernetes.io/name: litemaas
        app.kubernetes.io/instance: litemaas
        app.kubernetes.io/component: database
    spec:
      serviceAccountName: litemaas
      volumes:
        - name: postgres-init-script
          configMap:
            name: litemaas-postgresql-init
            defaultMode: 0755
      containers:
        - name: postgres
          image: "postgres:16-alpine"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              value: "litemaas_db"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: litemaas-postgresql
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: litemaas-postgresql
                  key: password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
            - name: postgres-init-script
              mountPath: /docker-entrypoint-initdb.d
              readOnly: true
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: postgres-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
---
# Source: litemaas/templates/frontend-route.yaml
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: litemaas
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: web
spec:
  to:
    kind: Service
    name: litemaas
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
---
# Source: litemaas/templates/litellm-route.yaml
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: litemaas-litellm
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
    app.kubernetes.io/name: litemaas
    app.kubernetes.io/instance: litemaas
    app.kubernetes.io/component: ai-proxy
spec:
  to:
    kind: Service
    name: litemaas-litellm
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
---
# Source: litemaas/templates/hook-rbac.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: litemaas-hook
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade,post-install,post-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
---
# Source: litemaas/templates/hook-rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: litemaas-hook
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade,post-install,post-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
rules:
  - apiGroups: ["route.openshift.io"]
    resources: ["routes"]
    verbs: ["get"]
  - apiGroups: [""]
    resources: ["serviceaccounts"]
    verbs: ["get", "patch"]
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "patch"]
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "patch"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch"]
---
# Source: litemaas/templates/hook-rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: litemaas-hook
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade,post-install,post-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
subjects:
  - kind: ServiceAccount
    name: litemaas-hook
    namespace: litemaas
roleRef:
  kind: Role
  name: litemaas-hook
  apiGroup: rbac.authorization.k8s.io
---
# Source: litemaas/templates/hook-patch-oauth.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: litemaas-oauth-setup
  labels:
    helm.sh/chart: litemaas-0.1.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: litemaas
    app.kubernetes.io/version: "0.3.1"
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 5
  template:
    metadata:
      labels:
        helm.sh/chart: litemaas-0.1.0
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: litemaas
        app.kubernetes.io/version: "0.3.1"
    spec:
      serviceAccountName: litemaas-hook
      restartPolicy: OnFailure
      securityContext:
        runAsNonRoot: true
      containers:
        - name: oauth-setup
          image: "bitnami/kubectl:latest"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          env:
            - name: ROUTE_NAME
              value: "litemaas"
            - name: SA_NAME
              value: "litemaas"
            - name: SECRET_NAME
              value: "litemaas-backend"
            - name: DEPLOY_NAME
              value: "litemaas-backend"
            - name: NAMESPACE
              value: "litemaas"
            - name: HAS_EXISTING_SECRET
              value: "false"
            - name: EXPLICIT_ISSUER
              value: ""
            - name: EXPLICIT_CALLBACK
              value: ""
            - name: CONFIGMAP_NAME
              value: litemaas-backend-config
            - name: LITELLM_ROUTE_NAME
              value: "litemaas-litellm"
            - name: LITELLM_ROUTE_ENABLED
              value: "true"
          command:
            - /bin/sh
            - -c
            - |
              set -e

              echo "=== LiteMaaS OAuth Setup Hook ==="
              echo "Route:      $ROUTE_NAME"
              echo "SA:         $SA_NAME"
              echo "Secret:     $SECRET_NAME"
              echo "Deployment: $DEPLOY_NAME"
              echo ""

              # ── Step 1: Wait for Route hostname ──────────────────────────
              echo "Step 1: Waiting for Route hostname..."
              ROUTE_HOST=""
              attempts=0
              max_attempts=30

              while [ -z "$ROUTE_HOST" ]; do
                  if [ "$attempts" -ge "$max_attempts" ]; then
                      echo "ERROR: Route host not assigned after $max_attempts attempts"
                      exit 1
                  fi
                  ROUTE_HOST=$(kubectl get route "$ROUTE_NAME" -n "$NAMESPACE" \
                      -o jsonpath='{.spec.host}' 2>/dev/null || true)
                  if [ -z "$ROUTE_HOST" ]; then
                      echo "  Waiting... (attempt $((attempts + 1))/$max_attempts)"
                      sleep 2
                      attempts=$((attempts + 1))
                  fi
              done
              echo "  Found: $ROUTE_HOST"
              echo ""

              # ── Step 2: Derive values ────────────────────────────────────
              CALLBACK_URL="https://${ROUTE_HOST}/api/auth/callback"
              CORS_ORIGIN="https://${ROUTE_HOST}"

              # Use explicit values when provided
              if [ -n "$EXPLICIT_CALLBACK" ]; then
                  CALLBACK_URL="$EXPLICIT_CALLBACK"
              fi

              # Derive OAuth issuer from the auto-generated hostname
              # OpenShift format: <route-name>-<namespace>.<apps-domain>
              OAUTH_ISSUER="$EXPLICIT_ISSUER"
              if [ -z "$OAUTH_ISSUER" ]; then
                  PREFIX="${ROUTE_NAME}-${NAMESPACE}."
                  APPS_DOMAIN="${ROUTE_HOST#${PREFIX}}"
                  if [ "$APPS_DOMAIN" != "$ROUTE_HOST" ]; then
                      OAUTH_ISSUER="https://oauth-openshift.${APPS_DOMAIN}"
                      echo "  Detected apps domain: $APPS_DOMAIN"
                  else
                      echo "  WARNING: Could not derive apps domain from hostname."
                      echo "  Set oauth.issuer explicitly if OAuth login fails."
                  fi
              fi

              echo "Step 2: Values:"
              echo "  Callback URL:  $CALLBACK_URL"
              echo "  CORS origin:   $CORS_ORIGIN"
              echo "  OAuth issuer:  ${OAUTH_ISSUER:-(not resolved)}"
              echo ""

              # ── Step 3: Patch ServiceAccount ─────────────────────────────
              echo "Step 3: Patching ServiceAccount annotation..."
              kubectl annotate serviceaccount "$SA_NAME" -n "$NAMESPACE" \
                  "serviceaccounts.openshift.io/oauth-redirecturi.litemaas=${CALLBACK_URL}" \
                  --overwrite
              echo "  Done."
              echo ""

              # ── Step 4: Patch backend Secret ─────────────────────────────
              NEED_RESTART="false"
              if [ "$HAS_EXISTING_SECRET" = "true" ]; then
                  echo "Step 4: Skipped (using existingSecret)."
                  echo "  Ensure your Secret has correct cors-origin, oauth-issuer,"
                  echo "  and oauth-callback-url values."
              else
                  echo "Step 4: Patching backend Secret..."
                  CORS_B64=$(printf '%s' "$CORS_ORIGIN" | base64 | tr -d '\n')
                  CALLBACK_B64=$(printf '%s' "$CALLBACK_URL" | base64 | tr -d '\n')

                  if [ -n "$OAUTH_ISSUER" ]; then
                      ISSUER_B64=$(printf '%s' "$OAUTH_ISSUER" | base64 | tr -d '\n')
                      kubectl patch secret "$SECRET_NAME" -n "$NAMESPACE" --type merge \
                          -p "{\"data\":{\"cors-origin\":\"${CORS_B64}\",\"oauth-callback-url\":\"${CALLBACK_B64}\",\"oauth-issuer\":\"${ISSUER_B64}\"}}"
                  else
                      kubectl patch secret "$SECRET_NAME" -n "$NAMESPACE" --type merge \
                          -p "{\"data\":{\"cors-origin\":\"${CORS_B64}\",\"oauth-callback-url\":\"${CALLBACK_B64}\"}}"
                  fi
                  echo "  Done."
                  NEED_RESTART="true"
              fi
              echo ""

              # ── Step 4b: Patch LiteLLM URL in ConfigMap ─────────────────
              if [ "$LITELLM_ROUTE_ENABLED" = "true" ]; then
                  echo "Step 4b: Checking LiteLLM Route..."
                  LITELLM_HOST=""
                  if kubectl get route "$LITELLM_ROUTE_NAME" -n "$NAMESPACE" >/dev/null 2>&1; then
                      LITELLM_HOST=$(kubectl get route "$LITELLM_ROUTE_NAME" -n "$NAMESPACE" \
                          -o jsonpath='{.spec.host}' 2>/dev/null || true)
                  fi
                  if [ -n "$LITELLM_HOST" ]; then
                      LITELLM_URL="https://${LITELLM_HOST}"
                      echo "  Patching LITELLM_API_URL → $LITELLM_URL"
                      kubectl patch configmap "$CONFIGMAP_NAME" -n "$NAMESPACE" --type merge \
                          -p "{\"data\":{\"LITELLM_API_URL\":\"${LITELLM_URL}\"}}"
                      NEED_RESTART="true"
                      echo "  Done."
                  else
                      echo "  LiteLLM Route host not found; keeping template-time URL."
                  fi
              else
                  echo "Step 4b: Skipped (LiteLLM Route not enabled)."
              fi
              echo ""

              # ── Step 5: Restart backend ──────────────────────────────────
              if [ "$NEED_RESTART" = "true" ]; then
                  echo "Step 5: Restarting backend deployment..."
                  kubectl rollout restart deployment/"$DEPLOY_NAME" -n "$NAMESPACE"
                  echo "  Done."
              else
                  echo "Step 5: Skipped (no Secret changes)."
              fi

              echo ""
              echo "=== OAuth setup complete ==="
          resources:
            requests:
              memory: "64Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "100m"
